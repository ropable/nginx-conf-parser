#!/usr/bin/env python

from setuptools import setup
version = ('0.1.2')

setup(
    name='nginx-conf-parser',
    version=version,
    packages=['nginx_conf_parser'],
    description='Nginx configuration parser for Python',
)
